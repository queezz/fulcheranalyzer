[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fulcher-analyzer"
version = "0.2.0"
authors = [
  { name="A. K.", email="queezz@gmail.com" },
]
description = "Molecular hydrogen Fulcher-alpha emission analyzer: intensity-table I/O, Boltzmann fitting, and coronal model reconstruction."
readme = "Readme.md"
license = { file="LICENSE" }
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "matplotlib",
    "sympy",
    "tqdm",
]

[project.optional-dependencies]
dev = [
    "ipython",
    "ipykernel",
    "jinja2",
    "jupyter",
    "pytest",
]
spectrum = ["xarray"]
docs = [
    "mkdocs",
    "mkdocs-material",
    "pymdown-extensions",
    "mdx_truly_sane_lists",
    "mkdocstrings[python]>=0.25",
    "mkdocs-glightbox",
]

[project.urls]
"Homepage" = "https://github.com/queezz/fulcheranalyzer"

[project.scripts]
fulcher-analyze-batch = "fulcher_analyzer.batch_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/fulcher_analyzer"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
